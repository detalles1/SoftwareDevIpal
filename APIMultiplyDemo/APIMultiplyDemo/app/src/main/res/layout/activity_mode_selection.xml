<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="#A8E6A3">

    <!-- Back Button, matching style and size of the Exit button in MainActivity -->
    <!-- Same red color as the Exit button -->
    <!-- Matching text size of Exit button -->
    <!-- Padding to match Exit button style -->
    <Button
        android:id="@+id/btn_back"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:layout_margin="16dp"
        android:text="Back"
        android:textColor="#FFFFFF"
        android:backgroundTint="#FF8A80"
        android:textSize="30sp"
        android:padding="16dp" />

    <!-- Centered LinearLayout for side-by-side buttons -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:orientation="horizontal">

        <!-- Shuffle Button, matching width and text size to main activity buttons -->
        <Button
            android:id="@+id/btn_shuffle"
            android:layout_width="213dp"
            android:layout_height="wrap_content"
            android:layout_marginEnd="16dp"
            android:backgroundTint="#4FC3F7"
            android:padding="16dp"
            android:text="Shuffle"
            android:textColor="#FFFFFF"
            android:textSize="40sp" />

        <!-- In Order Button, matching width and text size to main activity buttons -->
        <Button
            android:id="@+id/btn_in_order"
            android:layout_width="212dp"
            android:layout_height="wrap_content"
            android:backgroundTint="#4FC3F7"
            android:padding="16dp"
            android:text="In Order"
            android:textColor="#FFFFFF"
            android:textSize="40sp" />
    </LinearLayout>
</RelativeLayout>
